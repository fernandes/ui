<!DOCTYPE html>
<html style="background-color: <%= params.dig(:lookbook, :display, :bg_color) || "white" %>">
<head>
  <title>Component Preview</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
  />
</head>

<body style="padding: 40px; min-height: 100vh;" class="flex align-center">
  <div
  class="preview flex min-h-[350px] w-full justify-center p-10 items-center"
  style="
    margin-left: auto;
    margin-right: auto;
    max-width: <%= params.dig(:lookbook, :display, :max_width) || "100%" %>;
    min-height: 100%;
  ">
    <%= yield %> <!-- rendered preview will be injected here -->
  </div>
</body>
</html>
