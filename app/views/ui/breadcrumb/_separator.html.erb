<%# Breadcrumb Separator Component - Visual divider between items %>
<%
  content ||= local_assigns.fetch(:content, nil)
  @classes = local_assigns.fetch(:classes, "")
  @attributes = local_assigns.fetch(:attributes, {})

  # Extend behavior for shared logic
  extend UI::BreadcrumbSeparatorBehavior

  # Get HTML attributes from behavior
  all_attributes = breadcrumb_separator_html_attributes.deep_merge(@attributes)

  # Capture block content if provided
  block_content = capture { yield } if block_given?
  custom_content = content || (block_content if block_content.present?)
-%>
<%= content_tag :li, all_attributes do %>
  <% if custom_content %>
    <%= custom_content %>
  <% else %>
    <%= lucide_icon("chevron-right") %>
  <% end %>
<% end %>
