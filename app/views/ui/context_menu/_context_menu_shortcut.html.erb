<%# Context Menu Shortcut Component - Keyboard shortcut display %>
<%
  content ||= local_assigns.fetch(:content, nil)
  @classes = local_assigns.fetch(:classes, "")
  @attributes = local_assigns.fetch(:attributes, {})

  # Extend behavior for shared logic
  extend UI::ContextMenu::ContextMenuShortcutBehavior

  # Get HTML attributes from behavior
  all_attributes = context_menu_shortcut_html_attributes.merge(@attributes)
-%>
<%= content_tag :span, all_attributes do %>
  <%= content || yield %>
<% end %>
