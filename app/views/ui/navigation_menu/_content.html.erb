<%# NavigationMenu Content Component - Dropdown panel %>
<%
  @viewport = local_assigns.fetch(:viewport, true)
  @classes = local_assigns.fetch(:classes, "")
  @attributes = local_assigns.fetch(:attributes, {})

  extend UI::NavigationMenuContentBehavior

  all_attributes = navigation_menu_content_html_attributes.deep_merge(@attributes)
-%>
<%= content_tag :div, all_attributes do %>
  <%= yield %>
<% end %>
