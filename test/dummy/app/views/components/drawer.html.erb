<div class="space-y-12">
  <div class="space-y-2">
    <h1 class="text-3xl font-bold">Drawer</h1>
    <p class="text-muted-foreground">
      A mobile-first component that slides in from the edge of the screen with gesture-based drag-to-close.
    </p>
  </div>

  <!-- ========================================
       DEFAULT (BOTTOM)
       ======================================== -->
  <section class="space-y-6">
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Default (Bottom)</h2>
      <p class="text-sm text-muted-foreground">Drawer slides in from the bottom with drag-to-close gesture.</p>
    </div>

    <!-- Phlex -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">Phlex</h3>
      <%= render UI::Drawer::Drawer.new do %>
        <%= render UI::Drawer::Trigger.new do %>
          Open Drawer (Phlex)
        <% end %>
        <%= render UI::Drawer::Overlay.new %>
        <%= render UI::Drawer::Content.new do %>
          <%= render UI::Drawer::Handle.new %>
          <%= render UI::Drawer::Header.new do %>
            <%= render UI::Drawer::Title.new { "Move Goal" } %>
            <%= render UI::Drawer::Description.new { "Set your daily activity goal." } %>
          <% end %>
          <div class="p-4">
            <div class="grid gap-4">
              <div class="space-y-2">
                <label class="text-sm font-medium">Goal (minutes)</label>
                <input type="number" value="350" class="w-full px-3 py-2 border rounded-md">
              </div>
            </div>
          </div>
          <%= render UI::Drawer::Footer.new do %>
            <%= render UI::Button::Button.new { "Submit" } %>
            <%= render UI::Drawer::Close.new { "Cancel" } %>
          <% end %>
        <% end %>
      <% end %>
    </div>

    <!-- ERB -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ERB</h3>
      <%= render "ui/drawer/drawer" do %>
        <%= render "ui/drawer/trigger" do %>
          Open Drawer (ERB)
        <% end %>
        <%= render "ui/drawer/overlay" %>
        <%= render "ui/drawer/content" do %>
          <%= render "ui/drawer/handle" %>
          <%= render "ui/drawer/header" do %>
            <%= render "ui/drawer/title" do %>
              Move Goal
            <% end %>
            <%= render "ui/drawer/description" do %>
              Set your daily activity goal.
            <% end %>
          <% end %>
          <div class="p-4">
            <div class="grid gap-4">
              <div class="space-y-2">
                <label class="text-sm font-medium">Goal (minutes)</label>
                <input type="number" value="350" class="w-full px-3 py-2 border rounded-md">
              </div>
            </div>
          </div>
          <%= render "ui/drawer/footer" do %>
            <%= render "ui/button/button", variant: :default do %>
              Submit
            <% end %>
            <%= render "ui/drawer/close" do %>
              Cancel
            <% end %>
          <% end %>
        <% end %>
      <% end %>
    </div>

    <!-- ViewComponent -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ViewComponent</h3>
      <%= render UI::Drawer::DrawerComponent.new do %>
        <%= render UI::Drawer::TriggerComponent.new do %>
          Open Drawer (ViewComponent)
        <% end %>
        <%= render UI::Drawer::OverlayComponent.new %>
        <%= render UI::Drawer::ContentComponent.new do %>
          <%= render UI::Drawer::HandleComponent.new %>
          <%= render UI::Drawer::HeaderComponent.new do %>
            <%= render UI::Drawer::TitleComponent.new do %>
              Move Goal
            <% end %>
            <%= render UI::Drawer::DescriptionComponent.new do %>
              Set your daily activity goal.
            <% end %>
          <% end %>
          <div class="p-4">
            <div class="grid gap-4">
              <div class="space-y-2">
                <label class="text-sm font-medium">Goal (minutes)</label>
                <input type="number" value="350" class="w-full px-3 py-2 border rounded-md">
              </div>
            </div>
          </div>
          <%= render UI::Drawer::FooterComponent.new do %>
            <%= render UI::Button::ButtonComponent.new do %>
              Submit
            <% end %>
            <%= render UI::Drawer::CloseComponent.new do %>
              Cancel
            <% end %>
          <% end %>
        <% end %>
      <% end %>
    </div>
  </section>

  <!-- ========================================
       DIRECTIONS
       ======================================== -->
  <section class="space-y-6">
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Directions</h2>
      <p class="text-sm text-muted-foreground">Drawer can slide from any edge: top, bottom, left, or right.</p>
    </div>

    <!-- Phlex -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">Phlex</h3>
      <div class="flex gap-4">
        <%= render UI::Drawer::Drawer.new(direction: "top") do %>
          <%= render UI::Drawer::Trigger.new do %>
            Top
          <% end %>
          <%= render UI::Drawer::Overlay.new %>
          <%= render UI::Drawer::Content.new(direction: "top") do %>
            <%= render UI::Drawer::Handle.new %>
            <%= render UI::Drawer::Header.new do %>
              <%= render UI::Drawer::Title.new { "From Top" } %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>

        <%= render UI::Drawer::Drawer.new(direction: "left") do %>
          <%= render UI::Drawer::Trigger.new do %>
            Left
          <% end %>
          <%= render UI::Drawer::Overlay.new %>
          <%= render UI::Drawer::Content.new(direction: "left") do %>
            <%= render UI::Drawer::Header.new do %>
              <%= render UI::Drawer::Title.new { "From Left" } %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>

        <%= render UI::Drawer::Drawer.new(direction: "right") do %>
          <%= render UI::Drawer::Trigger.new do %>
            Right
          <% end %>
          <%= render UI::Drawer::Overlay.new %>
          <%= render UI::Drawer::Content.new(direction: "right") do %>
            <%= render UI::Drawer::Header.new do %>
              <%= render UI::Drawer::Title.new { "From Right" } %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>
      </div>
    </div>

    <!-- ERB -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ERB</h3>
      <div class="flex gap-4">
        <%= render "ui/drawer/drawer", direction: "top" do %>
          <%= render "ui/drawer/trigger" do %>
            Top
          <% end %>
          <%= render "ui/drawer/overlay" %>
          <%= render "ui/drawer/content", direction: "top" do %>
            <%= render "ui/drawer/handle" %>
            <%= render "ui/drawer/header" do %>
              <%= render "ui/drawer/title" do %>
                From Top
              <% end %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>

        <%= render "ui/drawer/drawer", direction: "left" do %>
          <%= render "ui/drawer/trigger" do %>
            Left
          <% end %>
          <%= render "ui/drawer/overlay" %>
          <%= render "ui/drawer/content", direction: "left" do %>
            <%= render "ui/drawer/header" do %>
              <%= render "ui/drawer/title" do %>
                From Left
              <% end %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>

        <%= render "ui/drawer/drawer", direction: "right" do %>
          <%= render "ui/drawer/trigger" do %>
            Right
          <% end %>
          <%= render "ui/drawer/overlay" %>
          <%= render "ui/drawer/content", direction: "right" do %>
            <%= render "ui/drawer/header" do %>
              <%= render "ui/drawer/title" do %>
                From Right
              <% end %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>
      </div>
    </div>

    <!-- ViewComponent -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ViewComponent</h3>
      <div class="flex gap-4">
        <%= render UI::Drawer::DrawerComponent.new(direction: "top") do %>
          <%= render UI::Drawer::TriggerComponent.new do %>
            Top
          <% end %>
          <%= render UI::Drawer::OverlayComponent.new %>
          <%= render UI::Drawer::ContentComponent.new(direction: "top") do %>
            <%= render UI::Drawer::HandleComponent.new %>
            <%= render UI::Drawer::HeaderComponent.new do %>
              <%= render UI::Drawer::TitleComponent.new do %>
                From Top
              <% end %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>

        <%= render UI::Drawer::DrawerComponent.new(direction: "left") do %>
          <%= render UI::Drawer::TriggerComponent.new do %>
            Left
          <% end %>
          <%= render UI::Drawer::OverlayComponent.new %>
          <%= render UI::Drawer::ContentComponent.new(direction: "left") do %>
            <%= render UI::Drawer::HeaderComponent.new do %>
              <%= render UI::Drawer::TitleComponent.new do %>
                From Left
              <% end %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>

        <%= render UI::Drawer::DrawerComponent.new(direction: "right") do %>
          <%= render UI::Drawer::TriggerComponent.new do %>
            Right
          <% end %>
          <%= render UI::Drawer::OverlayComponent.new %>
          <%= render UI::Drawer::ContentComponent.new(direction: "right") do %>
            <%= render UI::Drawer::HeaderComponent.new do %>
              <%= render UI::Drawer::TitleComponent.new do %>
                From Right
              <% end %>
            <% end %>
            <div class="p-4">Content</div>
          <% end %>
        <% end %>
      </div>
    </div>
  </section>

  <!-- ========================================
       SNAP POINTS
       ======================================== -->
  <section class="space-y-6">
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Snap Points</h2>
      <p class="text-sm text-muted-foreground">Drawer with snap points for tiered content reveal (25%, 50%, 100%).</p>
    </div>

    <!-- Phlex -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">Phlex</h3>
      <%= render UI::Drawer::Drawer.new(modal: false, snap_points: [0.25, 0.5, 1], fade_from_index: 0) do %>
        <%= render UI::Drawer::Trigger.new do %>
          Snap Points (Phlex)
        <% end %>
        <%= render UI::Drawer::Overlay.new %>
        <%= render UI::Drawer::Content.new(classes: "min-h-screen") do %>
          <%= render UI::Drawer::Handle.new %>
          <%= render UI::Drawer::Header.new do %>
            <%= render UI::Drawer::Title.new { "Snap Points" } %>
            <%= render UI::Drawer::Description.new { "Drag to different heights." } %>
          <% end %>
          <div class="p-4 space-y-2">
            <p class="text-sm">First: 25% viewport</p>
            <p class="text-sm">Second: 50% viewport</p>
            <p class="text-sm">Third: 100% viewport</p>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- ERB -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ERB</h3>
      <%= render "ui/drawer/drawer", modal: false, snap_points: [0.25, 0.5, 1], fade_from_index: 0 do %>
        <%= render "ui/drawer/trigger" do %>
          Snap Points (ERB)
        <% end %>
        <%= render "ui/drawer/overlay" %>
        <%= render "ui/drawer/content", classes: "min-h-screen" do %>
          <%= render "ui/drawer/handle" %>
          <%= render "ui/drawer/header" do %>
            <%= render "ui/drawer/title" do %>
              Snap Points
            <% end %>
            <%= render "ui/drawer/description" do %>
              Drag to different heights.
            <% end %>
          <% end %>
          <div class="p-4 space-y-2">
            <p class="text-sm">First: 25% viewport</p>
            <p class="text-sm">Second: 50% viewport</p>
            <p class="text-sm">Third: 100% viewport</p>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- ViewComponent -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ViewComponent</h3>
      <%= render UI::Drawer::DrawerComponent.new(modal: false, snap_points: [0.25, 0.5, 1], fade_from_index: 0) do %>
        <%= render UI::Drawer::TriggerComponent.new do %>
          Snap Points (VC)
        <% end %>
        <%= render UI::Drawer::OverlayComponent.new %>
        <%= render UI::Drawer::ContentComponent.new(classes: "min-h-screen") do %>
          <%= render UI::Drawer::HandleComponent.new %>
          <%= render UI::Drawer::HeaderComponent.new do %>
            <%= render UI::Drawer::TitleComponent.new do %>
              Snap Points
            <% end %>
            <%= render UI::Drawer::DescriptionComponent.new do %>
              Drag to different heights.
            <% end %>
          <% end %>
          <div class="p-4 space-y-2">
            <p class="text-sm">First: 25% viewport</p>
            <p class="text-sm">Second: 50% viewport</p>
            <p class="text-sm">Third: 100% viewport</p>
          </div>
        <% end %>
      <% end %>
    </div>
  </section>

  <!-- ========================================
       HANDLE-ONLY DRAGGING
       ======================================== -->
  <section class="space-y-6">
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Handle-Only Dragging</h2>
      <p class="text-sm text-muted-foreground">Only the handle is draggable - content area is scrollable.</p>
    </div>

    <!-- Phlex -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">Phlex</h3>
      <%= render UI::Drawer::Drawer.new(handle_only: true) do %>
        <%= render UI::Drawer::Trigger.new do %>
          Handle-Only (Phlex)
        <% end %>
        <%= render UI::Drawer::Overlay.new %>
        <%= render UI::Drawer::Content.new do %>
          <%= render UI::Drawer::Handle.new %>
          <%= render UI::Drawer::Header.new do %>
            <%= render UI::Drawer::Title.new { "Handle-Only Mode" } %>
            <%= render UI::Drawer::Description.new { "Only the handle is draggable." } %>
          <% end %>
          <div class="p-4 space-y-2" data-vaul-scrollable>
            <p class="text-sm">Drag the handle to close.</p>
            <% 5.times do |i| %>
              <p class="text-sm text-muted-foreground">Scrollable item <%= i + 1 %></p>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- ERB -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ERB</h3>
      <%= render "ui/drawer/drawer", handle_only: true do %>
        <%= render "ui/drawer/trigger" do %>
          Handle-Only (ERB)
        <% end %>
        <%= render "ui/drawer/overlay" %>
        <%= render "ui/drawer/content" do %>
          <%= render "ui/drawer/handle" %>
          <%= render "ui/drawer/header" do %>
            <%= render "ui/drawer/title" do %>
              Handle-Only Mode
            <% end %>
            <%= render "ui/drawer/description" do %>
              Only the handle is draggable.
            <% end %>
          <% end %>
          <div class="p-4 space-y-2" data-vaul-scrollable>
            <p class="text-sm">Drag the handle to close.</p>
            <% 5.times do |i| %>
              <p class="text-sm text-muted-foreground">Scrollable item <%= i + 1 %></p>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- ViewComponent -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ViewComponent</h3>
      <%= render UI::Drawer::DrawerComponent.new(handle_only: true) do %>
        <%= render UI::Drawer::TriggerComponent.new do %>
          Handle-Only (VC)
        <% end %>
        <%= render UI::Drawer::OverlayComponent.new %>
        <%= render UI::Drawer::ContentComponent.new do %>
          <%= render UI::Drawer::HandleComponent.new %>
          <%= render UI::Drawer::HeaderComponent.new do %>
            <%= render UI::Drawer::TitleComponent.new do %>
              Handle-Only Mode
            <% end %>
            <%= render UI::Drawer::DescriptionComponent.new do %>
              Only the handle is draggable.
            <% end %>
          <% end %>
          <div class="p-4 space-y-2" data-vaul-scrollable>
            <p class="text-sm">Drag the handle to close.</p>
            <% 5.times do |i| %>
              <p class="text-sm text-muted-foreground">Scrollable item <%= i + 1 %></p>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>
  </section>

  <!-- ========================================
       NON-MODAL MODE
       ======================================== -->
  <section class="space-y-6">
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Non-Modal Mode</h2>
      <p class="text-sm text-muted-foreground">Background remains interactive when drawer is open.</p>
    </div>

    <!-- Phlex -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">Phlex</h3>
      <%= render UI::Drawer::Drawer.new(modal: false) do %>
        <%= render UI::Drawer::Trigger.new do %>
          Non-Modal (Phlex)
        <% end %>
        <%= render UI::Drawer::Overlay.new %>
        <%= render UI::Drawer::Content.new do %>
          <%= render UI::Drawer::Handle.new %>
          <%= render UI::Drawer::Header.new do %>
            <%= render UI::Drawer::Title.new { "Non-Modal" } %>
            <%= render UI::Drawer::Description.new { "Page remains interactive." } %>
          <% end %>
          <div class="p-4">
            <p class="text-sm">Try clicking the page behind this drawer.</p>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- ERB -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ERB</h3>
      <%= render "ui/drawer/drawer", modal: false do %>
        <%= render "ui/drawer/trigger" do %>
          Non-Modal (ERB)
        <% end %>
        <%= render "ui/drawer/overlay" %>
        <%= render "ui/drawer/content" do %>
          <%= render "ui/drawer/handle" %>
          <%= render "ui/drawer/header" do %>
            <%= render "ui/drawer/title" do %>
              Non-Modal
            <% end %>
            <%= render "ui/drawer/description" do %>
              Page remains interactive.
            <% end %>
          <% end %>
          <div class="p-4">
            <p class="text-sm">Try clicking the page behind this drawer.</p>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- ViewComponent -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ViewComponent</h3>
      <%= render UI::Drawer::DrawerComponent.new(modal: false) do %>
        <%= render UI::Drawer::TriggerComponent.new do %>
          Non-Modal (VC)
        <% end %>
        <%= render UI::Drawer::OverlayComponent.new %>
        <%= render UI::Drawer::ContentComponent.new do %>
          <%= render UI::Drawer::HandleComponent.new %>
          <%= render UI::Drawer::HeaderComponent.new do %>
            <%= render UI::Drawer::TitleComponent.new do %>
              Non-Modal
            <% end %>
            <%= render UI::Drawer::DescriptionComponent.new do %>
              Page remains interactive.
            <% end %>
          <% end %>
          <div class="p-4">
            <p class="text-sm">Try clicking the page behind this drawer.</p>
          </div>
        <% end %>
      <% end %>
    </div>
  </section>

  <!-- ========================================
       RESPONSIVE DIALOG
       ======================================== -->
  <section class="space-y-6">
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Responsive Dialog</h2>
      <p class="text-sm text-muted-foreground">
        Shows Dialog on desktop (≥768px) and Drawer on mobile (&lt;768px). Resize browser to see it switch!
      </p>
    </div>

    <!-- Phlex -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">Phlex</h3>
      <div data-controller="ui--responsive-dialog" data-ui--responsive-dialog-breakpoint-value="768">
        <div class="md:hidden" data-ui--responsive-dialog-target="drawer">
          <%= render UI::Drawer::Drawer.new do %>
            <%= render UI::Drawer::Trigger.new do %>
              Edit Profile (Mobile)
            <% end %>
            <%= render UI::Drawer::Overlay.new %>
            <%= render UI::Drawer::Content.new do %>
              <%= render UI::Drawer::Handle.new %>
              <%= render UI::Drawer::Header.new(classes: "text-left") do %>
                <%= render UI::Drawer::Title.new { "Edit profile" } %>
                <%= render UI::Drawer::Description.new { "Make changes to your profile." } %>
              <% end %>
              <div class="grid gap-4 px-4">
                <div class="space-y-2">
                  <%= render UI::Label::Label.new(for: "name-phlex") { "Name" } %>
                  <%= render UI::Input::Input.new(id: "name-phlex", value: "Pedro Duarte") %>
                </div>
              </div>
              <%= render UI::Drawer::Footer.new(classes: "pt-2") do %>
                <%= render UI::Button::Button.new { "Save" } %>
                <%= render UI::Drawer::Close.new { "Cancel" } %>
              <% end %>
            <% end %>
          <% end %>
        </div>
        <div class="hidden md:block" data-ui--responsive-dialog-target="dialog">
          <%= render UI::Dialog::Dialog.new do %>
            <%= render UI::Dialog::Trigger.new do %>
              Edit Profile (Desktop)
            <% end %>
            <%= render UI::Dialog::Overlay.new %>
            <%= render UI::Dialog::Content.new do %>
              <%= render UI::Dialog::Header.new do %>
                <%= render UI::Dialog::Title.new { "Edit profile" } %>
                <%= render UI::Dialog::Description.new { "Make changes to your profile." } %>
              <% end %>
              <div class="grid gap-4 py-4">
                <div class="grid grid-cols-4 items-center gap-4">
                  <%= render UI::Label::Label.new(for: "name-phlex-d", classes: "text-right") { "Name" } %>
                  <%= render UI::Input::Input.new(id: "name-phlex-d", value: "Pedro Duarte", classes: "col-span-3") %>
                </div>
              </div>
              <%= render UI::Dialog::Footer.new do %>
                <%= render UI::Button::Button.new { "Save" } %>
              <% end %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>

    <!-- ERB -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ERB</h3>
      <div data-controller="ui--responsive-dialog" data-ui--responsive-dialog-breakpoint-value="768">
        <div class="md:hidden" data-ui--responsive-dialog-target="drawer">
          <%= render "ui/drawer/drawer" do %>
            <%= render "ui/drawer/trigger" do %>
              Edit Profile (Mobile)
            <% end %>
            <%= render "ui/drawer/overlay" %>
            <%= render "ui/drawer/content" do %>
              <%= render "ui/drawer/handle" %>
              <%= render "ui/drawer/header", classes: "text-left" do %>
                <%= render "ui/drawer/title" do %>
                  Edit profile
                <% end %>
                <%= render "ui/drawer/description" do %>
                  Make changes to your profile.
                <% end %>
              <% end %>
              <div class="grid gap-4 px-4">
                <div class="space-y-2">
                  <%= render "ui/label/label", for: "name-erb" do %>
                    Name
                  <% end %>
                  <%= render "ui/input/input", id: "name-erb", value: "Pedro Duarte" %>
                </div>
              </div>
              <%= render "ui/drawer/footer", classes: "pt-2" do %>
                <%= render "ui/button/button" do %>
                  Save
                <% end %>
                <%= render "ui/drawer/close" do %>
                  Cancel
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>
        <div class="hidden md:block" data-ui--responsive-dialog-target="dialog">
          <%= render UI::Dialog::Dialog.new do %>
            <%= render UI::Dialog::Trigger.new do %>
              Edit Profile (Desktop)
            <% end %>
            <%= render UI::Dialog::Overlay.new %>
            <%= render UI::Dialog::Content.new do %>
              <%= render UI::Dialog::Header.new do %>
                <%= render UI::Dialog::Title.new { "Edit profile" } %>
                <%= render UI::Dialog::Description.new { "Make changes to your profile." } %>
              <% end %>
              <div class="grid gap-4 py-4">
                <div class="grid grid-cols-4 items-center gap-4">
                  <%= render UI::Label::Label.new(for: "name-erb-d", classes: "text-right") { "Name" } %>
                  <%= render UI::Input::Input.new(id: "name-erb-d", value: "Pedro Duarte", classes: "col-span-3") %>
                </div>
              </div>
              <%= render UI::Dialog::Footer.new do %>
                <%= render UI::Button::Button.new { "Save" } %>
              <% end %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>

    <!-- ViewComponent -->
    <div class="space-y-2">
      <h3 class="text-lg font-medium">ViewComponent</h3>
      <div data-controller="ui--responsive-dialog" data-ui--responsive-dialog-breakpoint-value="768">
        <div class="md:hidden" data-ui--responsive-dialog-target="drawer">
          <%= render UI::Drawer::DrawerComponent.new do %>
            <%= render UI::Drawer::TriggerComponent.new do %>
              Edit Profile (Mobile)
            <% end %>
            <%= render UI::Drawer::OverlayComponent.new %>
            <%= render UI::Drawer::ContentComponent.new do %>
              <%= render UI::Drawer::HandleComponent.new %>
              <%= render UI::Drawer::HeaderComponent.new(classes: "text-left") do %>
                <%= render UI::Drawer::TitleComponent.new do %>
                  Edit profile
                <% end %>
                <%= render UI::Drawer::DescriptionComponent.new do %>
                  Make changes to your profile.
                <% end %>
              <% end %>
              <div class="grid gap-4 px-4">
                <div class="space-y-2">
                  <%= render UI::Label::LabelComponent.new(for: "name-vc") do %>
                    Name
                  <% end %>
                  <%= render UI::Input::InputComponent.new(id: "name-vc", value: "Pedro Duarte") %>
                </div>
              </div>
              <%= render UI::Drawer::FooterComponent.new(classes: "pt-2") do %>
                <%= render UI::Button::ButtonComponent.new do %>
                  Save
                <% end %>
                <%= render UI::Drawer::CloseComponent.new do %>
                  Cancel
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>
        <div class="hidden md:block" data-ui--responsive-dialog-target="dialog">
          <%= render UI::Dialog::Dialog.new do %>
            <%= render UI::Dialog::Trigger.new do %>
              Edit Profile (Desktop)
            <% end %>
            <%= render UI::Dialog::Overlay.new %>
            <%= render UI::Dialog::Content.new do %>
              <%= render UI::Dialog::Header.new do %>
                <%= render UI::Dialog::Title.new { "Edit profile" } %>
                <%= render UI::Dialog::Description.new { "Make changes to your profile." } %>
              <% end %>
              <div class="grid gap-4 py-4">
                <div class="grid grid-cols-4 items-center gap-4">
                  <%= render UI::Label::Label.new(for: "name-vc-d", classes: "text-right") { "Name" } %>
                  <%= render UI::Input::Input.new(id: "name-vc-d", value: "Pedro Duarte", classes: "col-span-3") %>
                </div>
              </div>
              <%= render UI::Dialog::Footer.new do %>
                <%= render UI::Button::Button.new { "Save" } %>
              <% end %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </section>

  <!-- ========================================
       IMPLEMENTATION NOTES
       ======================================== -->
  <section class="space-y-4">
    <div class="space-y-2">
      <h2 class="text-2xl font-semibold">Implementation Notes</h2>
    </div>

    <div class="space-y-4">
      <div>
        <h3 class="font-semibold mb-2">Features</h3>
        <ul class="list-disc list-inside space-y-1 text-sm text-muted-foreground">
          <li>Drag-to-close gesture with velocity detection</li>
          <li>Four directions: bottom, top, left, right</li>
          <li>Snap points for tiered content reveal</li>
          <li>Modal and non-modal modes</li>
          <li>Handle-only dragging mode</li>
          <li>Input repositioning for mobile keyboards</li>
          <li>Responsive dialog pattern (Dialog on desktop, Drawer on mobile)</li>
        </ul>
      </div>

      <div>
        <h3 class="font-semibold mb-2">Accessibility</h3>
        <ul class="list-disc list-inside space-y-1 text-sm text-muted-foreground">
          <li>ARIA attributes (role="dialog", aria-modal)</li>
          <li>Focus trap in modal mode</li>
          <li>Escape key to close (when dismissible)</li>
          <li>Keyboard navigation</li>
        </ul>
      </div>

      <div>
        <h3 class="font-semibold mb-2">Based on Vaul</h3>
        <p class="text-sm text-muted-foreground">
          This implementation is based on <a href="https://vaul.emilkowal.ski" target="_blank" class="underline">Vaul</a>,
          a mobile-first drawer component for React, ported to Rails with Stimulus.
        </p>
      </div>
    </div>
  </section>
</div>
